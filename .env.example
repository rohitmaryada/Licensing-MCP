# Copy to .env and fill in. .env is git-ignored — never commit the real key.
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here
CS_ACTOR_ID=rep.sarah@mathworks.com

# Local Postgres (db/docker-compose.yml) for host processes, e.g. scripts/generate_bulk.py.
# Services running inside Docker (services/docker-compose.yml) override this to use
# host.docker.internal instead of localhost — see services/docker-compose.yml.
LICENSING_PG_URL=postgresql://licensing:licensing@localhost:5433/licensing
