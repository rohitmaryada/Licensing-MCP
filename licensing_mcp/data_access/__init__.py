# Data access layer — SQLAlchemy queries standing in for microservice REST calls.
# In production, each query function body becomes an authenticated HTTP call.
# Function signatures are stable across POC and production.
